spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://config:8888
      retry:
        initial-interval: 30000
        max-interval: 30000
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user
