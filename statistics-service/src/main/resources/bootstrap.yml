spring:
  application:
    name: statistics-service
  cloud:
    config:
      uri: http://config:8888
      retry:
        initial-interval: 30000
        max-interval: 60000
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user