spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://config:8888
      retry:
        initial-interval: 20000
        max-interval: 30000
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user
